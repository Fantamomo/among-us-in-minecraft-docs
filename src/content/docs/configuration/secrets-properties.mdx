---
title: secret.properties
description: Configuration file for sensitive credentials and secret keys.
---

:::danger
This file stores private credentials and secret keys. **Do not** share, upload, commit, or send this file to anyone. Exposure of this information may compromise your server's security.
:::

The `secret.properties` file is located at:

```
plugins/AmongUs/secret.properties
```

It contains sensitive credentials required for optional plugin features such as MorphBlender.

---

## Default File Content

```properties
# CONFIDENTIAL FILE - CONTAINS SENSITIVE INFORMATION
# This file stores private credentials and secret keys.
# DO NOT share, upload, commit, or send this file to anyone.
# Exposure of this information may compromise security.

# API Key for https://mineskin.org/
# Used by the Morphling role to generate skins during transformation.
mineskin=
```

---

## MineSkin API Key

The `mineskin` property stores your API key for [mineskin.org](https://mineskin.org/). It is required for MorphBlender skin animations and [Morphling](/roles/imposters/morphling) transformations. Without it, the Morphling role falls back to instant skin switching and no skins are generated dynamically.

### How to Obtain an API Key

1. Create an account on [MineSkin](https://account.mineskin.org).
2. Generate a new API key in your dashboard:
   - Give your key a descriptive name.
   - Select **"A plugin or mod asked me to create an API key"**.
   - Optionally, set the project link to `https://github.com/Fantamomo/among-us-in-minecraft/`.
3. Paste the key into the file:

    ```properties
    mineskin=YOUR_API_KEY_HERE
    ```

4. Restart your server for the change to take effect.

:::note
MineSkin offers paid plans, but the free plan is sufficient for this plugin to function. The main benefit of a paid plan is faster skin generation — on the free plan, to generate a morph transformation around **36 seconds** are needed. The plugin will print the estimated generation time to the console on server start.
:::

---

## Security Best Practices

:::caution
Never upload this file to GitHub, public repositories, file hosting services, support tickets, or Discord servers.
:::

---

## What Happens If the Key Is Missing?

If `mineskin` is left empty, MorphBlender will not function and Morphling transformations will fall back to instant skin switching. All other plugin features will continue to work normally.