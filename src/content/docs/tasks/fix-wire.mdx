---
title: Fix Wires
---

import { Badge } from '@astrojs/starlight/components';

| Type                                   | Id         |
|----------------------------------------|------------|
| <Badge text="Long" variant="danger" /> | `fix_wire` |

In this task, the player must connect wires by matching colors in each row of the grid interface.

Editable slots appear in each row, and the player must fill them with the correct color to match the target row.

The task has multiple steps, with each step appearing at a new location. Completing all steps finishes the task.