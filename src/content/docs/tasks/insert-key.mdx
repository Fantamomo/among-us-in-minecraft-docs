---
title: Insert Key
---

import { Badge } from '@astrojs/starlight/components';

| Type                                      | Id           |
|-------------------------------------------|--------------|
| <Badge text="Common" variant="success" /> | `insert_key` |

In this task, the player must insert a key into the correct slot.

The interface shows multiple target slots and a movable key. Drag and place the key into the correct slot to complete the task.

Selecting the wrong slot will not progress the task, so the player must find the correct one.